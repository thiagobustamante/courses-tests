openapi: 3.0.1
info:
  title: Orders API
  description: API para criação e manipulação de orders.
  license:
    name: MIT
  version: 1.0.0
servers:
- url: /
paths:
  /orders:
    post:
      tags:
      - Orders
      description: Cria uma nova order
      operationId: createOrder
      requestBody:
        description: Dados da ordem a ser criada
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/Order'
        required: true
      responses:
        201:
          description: Ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/Order'
      x-eov-operation-id: createOrder
      x-eov-operation-handler: orders-endpoints
  /orders/{orderId}/items:
    post:
      tags:
      - Orders
      description: Cria um item de compra na order
      operationId: createOrderItem
      parameters:
      - name: orderId
        in: path
        description: Ordem a ser modificada
        required: true
        schema:
          type: string
      requestBody:
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/OrderItem'
        required: true
      responses:
        200:
          description: Ok
          content:
            application/json:
              schema:
                $ref: '#/components/schemas/OrderItem'
      x-eov-operation-id: createOrderItem
      x-eov-operation-handler: orders-endpoints
components:
  schemas:
    OrderItem:
      required:
      - amount
      - productId
      type: object
      properties:
        productId:
          type: string
          description: ""
        amount:
          type: number
          description: ""
          format: int32
      description: ""
    Order:
      required:
      - customerId
      type: object
      properties:
        customerId:
          type: string
          description: ""
        id:
          type: string
          description: ""
        items:
          type: array
          description: ""
          items:
            $ref: '#/components/schemas/OrderItem'
      description: ""
  securitySchemes:
    Bearer:
      type: apiKey
      name: Authorization
      in: header